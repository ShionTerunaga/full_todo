import ja from "../../../shared/ja";
const Title = () => {
    return (
        <div>
            <h1>{ja.home.title}</h1>
            <p>{ja.home.detail}</p>
        </div>
    );
}

export default Title;